{
  "name": "national-vitality-observatory",
  "version": "1.0.0",
  "private": true,
  "description": "National Vitality Observatory platform — login, settings and AI proxy around the observatory app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
